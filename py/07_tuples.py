# Tuples

coordinates = (10,20)
person = ("Alice", 25,"Engineer")
single_item = (42,)                 # Note the comma for single item

#Tuple operations
# person.append("Georgetown")
# single_item.insert(3,"Money")

print(coordinates[0])               # Acessing elements
print(len(person))                  # Length

